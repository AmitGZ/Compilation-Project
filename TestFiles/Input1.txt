program  Min  /* Finding minimum between two numbers */
start
decl real : b,c;
    int: a;
    string: d;
    
    b = 5;
    in(c);
    b = b * c;
    d="the result is ";
    out(d);
    if (a<b) then out(a);
	else   out(b);
end