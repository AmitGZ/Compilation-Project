program  Min  /* Finding minimum between two numbers */
start
decl real : b,c,e,f;
    int: a;
    string: d;
    
    in(a);
    in(b);
    d="the result is ";
    out(d);
    if (b < a) then out(a);
	else   out(b);
end