program  Min  /* Finding minimum between two numbers */
start
decl int : b,c,e,f, b;
    string: a, y, i;
final real amit = 5.8;
    in(b);
    in(c);
    if (gg > c) 
    then out(b);
    else     	
    out(c);

end