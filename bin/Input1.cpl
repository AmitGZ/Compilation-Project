program  Min  /* Finding minimum between two numbers */
start
decl int : b,c,e,f;
    string: a, y, i;
final real amit = 3.4;
    in(b);
    in(c);
    if (b > c) 
    then out(c);
    else     	
    out(c);

end