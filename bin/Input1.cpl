program  Min  /* Finding minimum between two numbers */
start
decl int : b,c,e,f;
    string: a, y, i;
final string amit = 5.2;
    in(b);
    in(c);
    if (gg > c) 
    then out(ggd);
    else     	
    out(c);

end